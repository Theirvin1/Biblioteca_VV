[pytest]
testpaths = tests
markers =
    requiere_setup_sql: pruebas que dependen de funciones, triggers y vistas de database/setup.sql (no solo de las tablas creadas por SQLAlchemy). Documenta la dependencia; tests/conftest.py ya aplica setup.sql automaticamente, asi que estas pruebas corren igual que las demas con "pytest".
